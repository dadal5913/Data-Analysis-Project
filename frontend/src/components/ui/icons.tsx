/**
 * Icon barrel — re-exports from `lucide-react` under the project's existing
 * `IconX` naming so pages/components don't need to be touched. For new code,
 * importing from `lucide-react` directly is also fine and fully shadcn-idiomatic.
 *
 * All icons accept the usual lucide props: `size`, `className`, `strokeWidth`, etc.
 */
export {
  LayoutDashboard as IconDashboard,
  Database as IconDatabase,
  LineChart as IconLineChart,
  Workflow as IconWorkflow,
  Sparkles as IconSparkles,
  Search as IconSearch,
  ChevronLeft as IconChevronLeft,
  ChevronRight as IconChevronRight,
  ChevronDown as IconChevronDown,
  ArrowUpRight as IconArrowUpRight,
  TrendingUp as IconTrendingUp,
  TrendingDown as IconTrendingDown,
  AlertCircle as IconAlertCircle,
  Info as IconInfo,
  Check as IconCheck,
  Loader2 as IconLoader,
  Upload as IconUpload,
  Play as IconPlay,
  LogOut as IconLogout,
  User as IconUser,
  ExternalLink as IconExternal,
  Zap as IconBolt,
  File as IconFile,
  Radio as IconRadio
} from "lucide-react";
